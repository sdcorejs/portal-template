import { Routes } from '@angular/router';
import { ApiConfiguration } from './configurations';
import { {{camelCase name}}Guard } from './guards';
import { SD_API_CONFIG } from '@sdcorejs/angular/services';

export const {{camelCase name}}Routes: Routes = [
  {
    path: '',
    canActivate: [{{camelCase name}}Guard],
    providers: [
      // Inject API Configuration ngay tại tầng module
      { provide: SD_API_CONFIG, useClass: ApiConfiguration, multi: true },
    ],
    children: [
      // Thêm các route của module tại đây
    ],
  },
];