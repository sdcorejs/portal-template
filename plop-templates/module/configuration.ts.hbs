import { InjectionToken } from '@angular/core';

export interface I{{properCase name}}Configuration {
  host: string;
}

export const {{constantCase name}}_CONFIGURATION = new InjectionToken<I{{properCase name}}Configuration>('{{dashCase name}}.configuration');
